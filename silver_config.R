silver_config <- list(
  # Maximum allowed % reduction during deduplication
  # If exceeded → pipeline fails
  max_dedup_reduction_pct = 5,

  # Maximum allowed lag between today and latest OperatingDate
  max_freshness_lag_days = 2
)